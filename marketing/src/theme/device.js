// Resolução LÓGICA da "tela do app" dentro do mockup de celular.
// As telas reproduzidas (src/screens/*) são desenhadas neste tamanho e depois
// escaladas pelo mockup — igual ao app rodando em 430pt de largura (iPhone-like).
export const DEVICE = {
  width: 430,
  height: 932,
  radius: 52, // canto da tela
}
